# Quality bundle 5 — FS checkpoints

**Do not place or use `shift_*/fs_checkpoints/` under this directory.**

Few-shot diffusion checkpoints live only under:

`../quality_bundle_4/shift_<sim_*>_3/fs_checkpoints/`

v5 eval sets `QUAL_FS_BUNDLE_ROOT=quality_bundle_4` in the pipeline.
